// small cubing process of 2*2*2=8 

#include <stdio.c>
#include <cs50.h>
int main(void)
{
    int x = 2;
    printf("x is now %i\n", x);
    printf("Cubing....\n");
    x = cube(x);
    printf("Cubed!\n");
    printf("x is now %i\n", x);
}
int main(int n)
{
       return n* n* n*;
}